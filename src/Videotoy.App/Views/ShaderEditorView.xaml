<UserControl x:Class="Videotoy.App.Views.ShaderEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d">
    <!--
        Fond et texte fixés en clair-sur-sombre indépendamment du thème
        clair de l'appli (Palette.xaml) : TextPrimaryBrush est un gris
        quasi noir prévu pour un fond clair, illisible sur
        ViewportBackgroundBrush (quasi noir) — les couleurs des .xshd
        (Resources/Editor/*.xshd) sont elles-mêmes calibrées pour un fond
        sombre, à la manière d'un thème d'IDE classique.
    -->
    <Grid Background="{StaticResource ViewportBackgroundBrush}">
        <avalonEdit:TextEditor x:Name="Editor"
                                FontFamily="Consolas"
                                FontSize="13"
                                ShowLineNumbers="True"
                                WordWrap="False"
                                Background="Transparent"
                                Foreground="#FFD4D4D4"
                                LineNumbersForeground="#FF6E7681"
                                Padding="8"
                                HorizontalScrollBarVisibility="Auto"
                                VerticalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
